type Override = {
  /** YYYY-MM-DD */
  start: string;
  /** YYYY-MM-DD */
  end: string;
  dev: string;
};

export const SERVE_BUG_ROTATION_OVERRIDES: {
  start: string;
  end: string;
  dev: string;
}[] = [];
